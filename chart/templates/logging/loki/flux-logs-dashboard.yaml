{{- if .Values.loki.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: flux-logs-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  flux-logs-dashboard.json: |
    {{ .Files.Get "dashboards/flux/logs.json" | nindent 4 }}
{{- end }}
